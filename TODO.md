# TODO (Report button mobile visibility)

- [x] Identify mobile report control behavior (reopen button vs report sidebar off-canvas).
- [ ] Fix mobile default state so the Report sidebar is visible by default (per user choice B).
- [ ] Verify on mobile breakpoint (<768px) that the report sidebar appears and the 📊 reopen button is not required.
